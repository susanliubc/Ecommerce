import React from 'react';
import CheckoutItem from '../CheckoutItem';

const Checkout = () => {
  return (
    <div>
      <h2>Checkout</h2>
      <CheckoutItem />
    </div>
  );
};

export default Checkout;
